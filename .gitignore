# Dependencies / build
node_modules
dist
dist-ssr

# ALL env files are ignored: the SERVICE KEY (secret) lives here.
# No .env file is ever committed in this tool.
.env
.env.*

# Logs / editor / OS
*.log
.vscode/*
.idea
.DS_Store
.vercel
